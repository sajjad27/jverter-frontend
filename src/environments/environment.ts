// const $host = "http://localhost:8080";

export const environment = {
  production: false,
  host: "http://localhost:8080",
  // authService: $host + "/auth/",
  // workersService: $host + "/workers/",
  // projectsService: $host + "/projects/",
  // workDay: $host + "/work-day/",
  // vacationService: $host + "/workers/{{workerId}}/vacations",
  // creditService: $host + "/workers/{{workerId}}/credits",
};

